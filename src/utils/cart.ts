// заглушка функции добавления в корзину
export const handleAddToCart = (id: number) => {
  console.log('add to cart', id);
};

// заглушка функции покупки
export const handleBuyNow = (id: number) => {
  console.log('buy now', id);
};
